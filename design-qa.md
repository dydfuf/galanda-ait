# 여행 중심 UI · 아이콘/에셋 QA

final result: passed

검증 범위는 여행 중심 홈, 내 여행, 여행방 계획/일정, 탐색/저장 목록, 마이의 주요
화면과 공통 아이콘·상태 일러스트다. 모든 상세/편집 화면을 새로 구현하거나 실기기
인증 흐름을 검증한 결과는 아니다. 아래 홈 시안 QA와 주요 화면 구현 확장을 구분한다.

## 비교 기준과 증거

- Source visual truth: `docs/assets/trip-led/home-reference.png` — 사용자가 선택한
  여행 중심 시안에 아이콘을 보완한 ImageGen 결과, 853 × 1844 px.
- Implementation: `http://127.0.0.1:5180/home`, 실제 React 화면과 로컬 샘플 API.
  실제 계정·DB를 사용하지 않았다. 샘플 여행은 가을 도쿄 여행, 일정 미정, 4명, 2개 후보다.
- Screenshot: `docs/assets/trip-led/home-light-390.png`, 390 × 844 px.
- CSS viewport 390 × 844, 캡처 밀도 1 px/CSS px. 프레임 없는 시안을 같은 크기로
  정규화하여 `docs/assets/trip-led/home-comparison.png`에 좌우로 합성하고 직접 검토했다.
- 비교 이미지에서 제목·아이콘·버튼이 원래 크기로 읽히므로 별도 확대 비교는 필요하지 않았다.

## Findings / 수정 이력

1. [P2, 수정 완료] 초기 홈의 제목 크기와 줄바꿈이 시안과 달랐다.
   `home-comparison-initial.png` 확인 후 제목을 48px, 1.15 행간, 단어 단위 줄바꿈으로
   조정했다. `home-comparison.png`에서 2줄 제목과 전체 구성 재비교 완료.
2. [P2, 수정 완료] 320px 빈 상태 제목의 마지막 글자가 혼자 다음 줄에 남았다.
   빈 상태 제목에 `break-keep` 적용. `home-empty-320.png`에서 수정 확인.
3. [P2, 수정 완료] 긴 제목의 화면에서 키보드로 진입한 주요 버튼 하단 약 8px가
   고정 내비게이션 아래에 놓였다. HomeNextAction의 버튼·링크에 내비게이션 높이를
   반영한 scroll margin을 적용했다. `home-long-320.png`에서 포커스와 버튼 전체 노출 확인.
   버튼 및 보조 링크의 하단 660.23px, 내비게이션 시작 675px로 각각 검증했다.
4. [P2, 수정 완료] 에셋 항목을 추가한 카탈로그 목차가 여러 줄로 늘어나 제목을 가렸다.
   기존 가로 스크롤 목차가 줄바꿈되지 않게 조정했다. `assets-light-390.png`,
   `assets-dark-390.png`에서 제목과 6개 그림의 배치 재확인.

미해결 P0/P1/P2 없음.

## 필수 시각 검토

| 항목 | 확인 결과 |
| --- | --- |
| 글꼴·타이포 | 기존 한국어 system sans 유지. 큰 여행명과 작은 메타데이터의 위계, 긴 제목 줄바꿈 확인. 시안의 작은 홈 라벨 대신 기존 PageTitle 크기를 유지하는 차이는 의도적이다. |
| 여백·배치 | 좌우 24px, 단일 여행, 한 개의 강조 CTA. 표준 제목에서 320/390/430px 가로 넘침 없음. 긴 내용은 세로 스크롤되며 하단 행동에 접근 가능하다. |
| 색상·토큰 | 기존 semantic token 유지. 라이트 primary의 계산값은 rgb(49,130,246). 시안의 래스터 파란색을 맞추려고 별도 브랜드 색을 만들지 않았다. 다크 모드 텍스트·아이콘·버튼 확인. |
| 이미지·아이콘 | 기존 Lucide와 원본 SVG만 사용. 128px에서 흐림·잘림 없음. 12개 SVG 모두 로드되며 테마별 6개만 표시된다. 로고와 앱 아이콘은 변경하지 않았다. |
| 문구·상태 | 일정 미정·실제 인원·후보 수 표시. 추천 실패 시 계획 진입 유지. 오류는 빈 여행으로 표현하지 않으며, 미확정 일정에는 확정 그림을 표시하지 않는다. |

시안과의 허용 차이: 메타데이터의 명확한 ‘참여’ 문구, 자연스러운 한 줄 상태 설명,
기존 PageTitle/브랜드 토큰, 브라우저의 OS safe-area 차이. 시안에 없는 홈 빈 상태와
초대·미확정 일정은 기존 제품 상태를 유지하면서 선택된 에셋을 배치했다.

## 동작·반응형 확인

- 390 × 844 라이트/다크: `home-light-390.png`, `home-dark-390.png`.
- 320 × 740: `home-light-320.png`, `home-long-320.png`, `home-empty-320.png`,
  `home-error-320.png`. 정상 여행은 화면 내 CTA 노출, 긴 제목은 세로 스크롤 허용.
- 430 × 932: DOM 측정상 문서 너비 430px, CTA 하단 772px, 내비게이션 시작 867px.
- 홈 ‘여행안 비교하기’ → 최신 여행 상태 확인 → 두 샘플 후보 비교 화면 → 뒤로 이동 확인.
- 오류 화면에는 상태 일러스트가 없으며 ‘다시 시도’로 여행 화면 복구 확인.
- Tab 이동으로 주요 버튼·보조 링크의 포커스 링과 전체 노출 확인.
- 초대 화면: `invite-dark-320.png`. 공유를 실행하지 않고 선택적 다음 단계 접근을 확인.
- 미확정 일정: `itinerary-unconfirmed-320.png`. 비교 일러스트와 후보 진입 버튼 확인.
- 홈→비교 동작 중 브라우저 오류·경고 없음. 오류 시나리오의 의도된 503은 별도이다.

## 검증과 잔여 범위

- 최종 `pnpm check`: lint, 146개 파일/1,451개 테스트, Drizzle 정합성,
  Web typecheck/build 및 Apps-in-Toss build 통과. 기존 lint 경고는 유지했다.
- `git diff --check` 통과.
- 일러스트 12개 총 11,716 bytes, 이번 추가분 8개 8,088 bytes.
  SVG가 기존 PWA precache 규칙에 포함되는 것을 소스로 확인했다.
- 실제 인증 계정의 브라우저 smoke, 설치형 PWA 오프라인 전환, Apps-in-Toss 실기기와
  키보드/safe-area 동작은 미검증. 로컬 AIT 빌드는 배포나 실기기 검증을 뜻하지 않는다.
- 서버 API·권한·DB 계약과 기존 저장 목록 경로를 유지했다. 이 QA는 PR 제출 전 로컬
  검증 기록이며 배포 결과가 아니다.

## Implementation checklist

- [x] 선택된 여행 중심 홈과 기존 상태 처리 통합
- [x] 아이콘 선 두께와 실사용 일러스트 통합
- [x] 시안/구현 합성 비교 및 발견된 P2 수정 후 재확인
- [x] 모바일·테마·빈 상태·오류·키보드 확인
- [x] 최종 저장소 검사

## 주요 화면 구현 확장 · 2026-09-08

홈에서 승인된 제목 중심·절제된 표면·단일 주요 행동의 문법을 실제 주요 화면에 적용했다.
새 의존성, 서버 API, DB, 권한 모델은 추가하지 않았다. 기존 Lucide, SVG,
`BottomAction`, `Tabs`, HTML `details`를 재사용했다.

### 적용 범위

- 내 여행: 진행/지난 여행을 분리하고 장식 그림·아바타 스택·FAB 대신 제목 중심 목록과
  하단 주요 버튼을 사용한다. 지난 여행의 기존 날짜 정렬과 상태 처리를 유지한다.
- 계획: 여행 요약과 후보의 중첩 카드를 덜어내고 참여 통계만 접는다. 예약 위험·강한
  의견·집계 제한 안내와 후보별 비용/응답/예약 근거는 계속 보인다. 추천은 하단 한 곳에
  배치하고 후보 0개에는 생성/이어서 쓰기 행동과 중복하지 않는다.
- 여행방: 하단의 떠 있는 모드 탭을 상단 헤더 아래로 옮겼다. 본문/주요 버튼과 겹치지
  않으며 기존 Web/native 헤더와 accessory 실패 fallback을 유지한다.
- 일정: 날짜 탭으로 하루씩 읽고 전체 경로는 펼쳐 본다. 미확인 예약과 revision 안내는
  접지 않는다. 선택 날짜가 갱신으로 사라지는 경우 첫 날짜로 복구한다.
- 탐색: 검색어 우선, 상세 필터는 적용 개수와 함께 접는다. 공개/저장 카드와 마이 메뉴를
  평평한 목록으로 정리한다. 저장/검색/테마의 기존 상태와 링크 경계를 유지한다.

### 최종 모바일 확인

모든 브라우저 확인은 실제 React 컴포넌트와 임시 로컬 샘플 API를 사용했다.
실제 인증·DB·검색 서버 검증이 아니다. 확정 일정 샘플은 이틀과 미확인 예약 2개,
공개 일정 샘플은 도쿄 일정 1개다. 샘플 검색 응답은 필터와 무관하게 동일하며,
필터 검증은 입력·표시·URL 동기화까지만 수행했다.

| 화면 | 캡처와 확인 |
| --- | --- |
| 내 여행 | `trips-before-390.png` → `trips-after-390.png`, `trips-after-320.png`. 현재/지난 탭 전환과 빈 상태 확인. 320px 가로 넘침 없고 생성 버튼이 글로벌 내비게이션 위에 노출된다. |
| 계획 | `plans-before-390.png` → `plans-after-390.png`, `plans-after-320.png`, `plans-bottom-320.png`. 상단 헤더/탭 높이 113px. 320px 하단 스크롤에서 마지막 후보 하단 520px, 주요 행동 영역 시작 560.375px로 내용 가림 없음. |
| 일정 | `itinerary-after-390.png`, `itinerary-day-320.png`. 선택한 날짜의 일정만 표시, 다른 날짜와 세부 Drawer 접근 확인. 방향키는 탭 포커스를 이동하고 Enter로 해당 날짜를 선택한다. 320px 가로 넘침 없음. |
| 탐색 | `explore-after-320.png`. 상세 필터 펼침/접힘, 테마 선택 후 검색 URL `?themeId=food`, 접힌 상태의 적용 1개 표시와 가로 넘침 없음 확인. |
| 마이 | `me-after-390.png`. 실제 프로필 이니셜과 저장 일정/화면 설정 메뉴 확인. |

일정/탐색은 320px 다크, 계획/목록/마이는 라이트에서도 확인했다. 모바일 가상 키보드와
실제 AIT WebView는 검증하지 않았다. 상세 화면 전체·실제 저장 mutation은 이번 브라우저
검증 범위가 아니다.

### 회귀 검사

- 참여 통계 disclosure 안팎의 정보 경계, 후보 0개에서 캐시된 추천 중복 방지,
  날짜 선택/선택 날짜 삭제 후 복구, 검색 URL/필터, 지난 여행 정렬,
  Web/AIT 헤더 소유권을 기존 Vitest 테스트에서 검증했다.
- 최종 `pnpm check` exit 0: 146개 파일, 1,450개 테스트, DB 정합성, Web/AIT build 통과.
  별도의 Playwright/E2E runner는 추가하지 않았다.
- 실제 인증 smoke, PWA 오프라인 전환, AIT 실기기 확인과 배포는 여전히 미수행이다.

## PR 피드백 반영: 헤더 그림자와 에셋 캡처

- 공통 chrome 스타일 중 `header`의 `box-shadow`만 제거했다. 라이트에서 계산된 그림자는
  `rgba(25, 31, 40, 0.12) 0px 8px 24px 0px` → `none`, 헤더 높이 57px와 경계선 1px는
  동일하다. 하단 행동 영역의 그림자와 overlay 스타일은 변경하지 않았다.
- 다크 카탈로그의 chrome 헤더 4개도 계산값 `none`을 확인했다.
- `plans-after-390.png`, `itinerary-after-390.png`, `assets-light-390.png`,
  `assets-dark-390.png`를 최신 헤더로 갱신했다. 이외 기존 캡처는 앞선 구현 시점 기록이다.
- 실제 앱에서 에셋이 표시되는 390 × 844px 샘플 화면 5개를 추가했다:
  `spot-empty-trips-390.png`(홈), `spot-empty-saved-390.png`(저장 목록),
  `spot-create-trip-390.png`(첫 여행안), `spot-invite-companions-390.png`(동행자 초대),
  `spot-compare-plans-390.png`(미확정 일정). 실제 공유·저장은 실행하지 않았다.
- `confirm-plan`은 라이트/다크 에셋 카탈로그에서만 시각 확인했다. 현재
  `ConfirmedItinerarySnapshotSchema`가 routes/items를 비어 있지 않은 배열로 제한하여,
  항목 없는 확정 일정 샘플은 클라이언트 검증에서 거부된다. 따라서 해당 방어용 빈 상태를
  정상 API로 재현했다고 주장하지 않는다. 이 도달성 차이는 별도 검토 대상으로 남기고,
  캡처를 위해 API 검증이나 실제 제품 상태를 변경하지 않았다.
- 수정 후 `pnpm check` exit 0: 146개 파일 / 1,450개 테스트 및 Web/AIT build 통과.

## PR 피드백 반영: 하단 고정 영역 한 종류

- Figma 없이 기존 UI를 수정했다. 전역 탐색 화면은 내비게이션만 고정하고,
  기존 생성·편집·확정 route는 전역 shell 밖에서 하단 액션만 소유한다.
- 내 여행의 생성 행동을 제목 옆으로 이동했다. 진행 중인 여행이 없는 정상 빈 상태는
  본문 안에 주요 생성 행동을 표시하고 제목 옆 행동은 생략한다. 지난 여행·조회 오류에서는
  제목 옆 행동을 유지한다. 고정 footer와 이를 위한 본문 여백은 제거했다.
- 홈의 정상/빈 상태/지난 여행 행동을 관련 본문 바로 뒤에 배치했다. 하단으로 밀어내던
  flex 여백을 없애고, 기존 추천 권한 확인·오류·진입 경로는 유지했다.
- 두 고정 영역의 동시 사용을 위한 nav offset/safe-area override를 제거했다.
  집중 작업의 키보드 inset과 safe-bottom, 액션 높이 측정/본문 여백 계약은 유지한다.

### 최신 캡처와 확인

모든 화면은 실제 React 컴포넌트와 로컬 샘플 API다. 이전 섹션의 홈/내 여행 캡처는
해당 구현 시점 기록이며, 아래가 단일 하단 영역을 적용한 최신 화면이다.

- `home-single-bottom-390.png`: 여행 정보에 이어 추천 행동을 배치.
  320px 다크에서도 버튼 하단 473.64px, 내비게이션 시작 675px이며 가로 넘침 없음.
- `home-empty-single-bottom-390.png`: 일러스트·설명 바로 뒤의 생성 행동.
  320px에서도 본문 너비 320px, 생성 버튼 하단 493.5px로 확인.
- `trips-single-bottom-390.png`, `trips-single-bottom-dark-390.png`,
  `trips-single-bottom-320.png`: 하단 액션 0개, 전역 내비게이션 1개. 제목 옆 생성 버튼의
  높이 44px 유지. 320px에서 제목/버튼이 같은 행에 있고 가로 넘침 없음.
- `trips-empty-single-bottom-390.png`: 본문 생성 버튼 1개, 고정 액션 0개.
- `create-single-bottom-320.png`: 내 여행의 생성 버튼으로 `/trips/new` 진입.
  전역 내비게이션 0개, 하단 액션 1개, 액션 하단 740px. 입력/생성 mutation은 실행하지 않았다.

### 검증

- 관련 6개 파일/79개 테스트 통과. 실제 `TripListPage`와 `GlobalAppShell` 조합에서
  생성 행동의 단일 노출·route 이동·footer 부재를 검증하고 기존 safe-area 테스트를 갱신했다.
- 최종 `pnpm check` exit 0: 146개 파일/1,451개 테스트, DB 정합성, Web/AIT build 통과.
- 실제 인증·PWA 오프라인·가상 키보드·AIT 실기기 검증과 배포는 수행하지 않았다.
